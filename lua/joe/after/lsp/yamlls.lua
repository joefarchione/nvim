return {
  settings = {
    yaml = {
      validate = true,
      schemaStore = {
        enable = true,
        url = "https://www.schemastore.org/api/json/catalog.json",
      },
      schemas = {},
      format = {
        enable = true,
      },
      hover = true,
      completion = true,
    },
  },
}
